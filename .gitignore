# dependencies
node_modules/
client/node_modules/

# build output
dist/
client/dist/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# receipt storage (bind-mounted volume)
# Anchored: an unanchored "storage/" also matches src/modules/receipts/storage/,
# which silently kept the storage driver itself out of the repo.
/storage/

# backups
backups/

# logs
*.log
npm-debug.log*

# test / coverage
coverage/

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Scratch worktrees created by Claude Code, never part of a commit.
.claude/worktrees/
