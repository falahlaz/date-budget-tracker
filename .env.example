NODE_ENV=development
PORT=3000
APP_TZ=Asia/Jakarta

DATABASE_URL=mysql://datebud:datebud@localhost:3306/datebud

# 64 hex chars: openssl rand -hex 32
JWT_SECRET=
JWT_ACCESS_TTL=15m
REFRESH_TTL_DAYS=30

STORAGE_DRIVER=local
STORAGE_ROOT=./storage
MAX_UPLOAD_MB=10
MAX_RECEIPTS_PER_EXPENSE=5

# Optional bootstrap: creates this user ONLY when the users table is empty
SEED_USER_EMAIL=
SEED_USER_PASSWORD=
SEED_USER_NAME=
